# Backend API base URL (chat, /db/skin_cancer, /db/one_percent_heat, /location/reverse)
# Set this in Vercel (Project → Settings → Environment Variables) to your deployed backend URL.
VITE_API_BASE=https://your-backend.example.com

# Optional: weather/UV API (default 8001 for local dev)
# VITE_WEATHER_API_BASE=https://your-weather-api.example.com
